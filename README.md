# FluxBeam

Engine de integração com a WhatsApp Business Platform em Java 21 + Spring Boot 3.

## Como executar

Configure as variáveis de ambiente indicadas em `application.yml` e execute:

```bash
mvn spring-boot:run
```
